import mongoose from 'mongoose';
import Users from '../models/users';

const create = (dto) => {
  const user = new Users({ _ });
};

const readAll = () => {
  Pizza.find({});
};
const readById = () => {};

const updateById = () => {};

const deleteById = () => {};
